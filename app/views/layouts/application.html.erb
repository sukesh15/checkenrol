<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "CheckEnrol" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>


    <!-- Javascripts
    ================================================== -->
    <%= javascript_include_tag "application" %>

  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href='/<%= "#{params[:organisation]}" %>'><img width="80px" src="<%= "/#{params[:organisation]}_logo.png" %>"> To check or update your enrolment, fill in your current address</a>
        </div>
      </div>
    </div>

      <div class="container">
        <div class="row">
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p><a href='/<%= "#{params[:organisation]}/check_enrolment/privacy" %>'>A note on Privacy</a></p>
        <% if params[:organisation] == 'greens'%>
          <p><small>Authorised by Ben Spies-Butcher and Christine Cunningham, Ground Floor, Unit 4, Jacobs House, 8-10 Hobart Place. Canberra City ACT.</small></p>
        <% end %>
      </footer>

    </div> <!-- /container -->
  </body>
</html>
